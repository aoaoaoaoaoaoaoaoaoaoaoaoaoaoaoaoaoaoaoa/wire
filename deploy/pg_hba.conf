# Local identities are the credential boundary. PostgreSQL never opens TCP.
local   all          postgres                 peer
local   mattermost   mattermost               peer
local   all          all                      reject
host    all          all          127.0.0.1/32 reject
host    all          all          ::1/128      reject
